# 🎯 Полная инструкция для администраторов CyberX

Комплексное руководство по всем административным функциям платформы CyberX Novokosino.

---

## 📚 Содержание

1. [Обзор платформы](#обзор-платформы)
2. [Social Hub - Социальная платформа](#social-hub)
3. [Arena - Система дуэлей](#arena)
4. [Управление ценами](#управление-ценами)
5. [Технические детали](#технические-детали)

---

## 🌐 Обзор платформы

### Что такое CyberX?

**CyberX** — это комплексная платформа для киберспортивных клубов с социальными функциями, системой дуэлей и управлением контентом.

### Основные модули:

1. **🎮 Social Hub** — социальная платформа для общения и вызовов
2. **⚔️ Arena** — система организации дуэлей между игроками
3. **💰 Admin Panel** — управление ценами и контентом сайта
4. **📡 Socket Server** — реал-тайм коммуникация

---

## 🎮 Social Hub

### Описание

Social Hub — это **социальная платформа в реальном времени**, где пользователи клубов могут:
- Общаться в чате
- Вызывать друг друга на дуэли
- Просматривать новостную ленту из Telegram
- Видеть онлайн пользователей

### 🔐 Админский вход

**URL**: 
- Продакшн: `https://cyberx-novokosino.ru/admin/login`
- Локально: `http://localhost:3000/admin/login`

**Процесс входа:**
1. Откройте `/admin/login`
2. Выберите клуб (Алтуфьево или Новокосино)
3. Введите пароль (из переменной `ADMIN_PASSWORD`)
4. Нажмите "АВТОРИЗОВАТЬСЯ"
5. Автоматическое перенаправление на `/social-hub`

### Возможности для администраторов

После авторизации админы могут:

✅ **Модерация чата**
- Просмотр всех сообщений в реал-тайм
- Управление каналами (общий, новости, промокоды)

✅ **Управление пользователями**
- Просмотр онлайн пользователей
- Отслеживание активности

✅ **Система вызовов**
- Наблюдение за дуэлями между игроками
- Получение уведомлений о активных challenge

✅ **Новостная лента**
- Автоматическая подгрузка из Telegram-каналов
- Последние 20 новостей

### Технические компоненты

**Основные файлы:**
- Админ-вход: `app/admin/login/page.tsx`
- Главная страница: `app/social-hub/page.tsx`
- Чат: `components/ChatFeed.tsx`
- Онлайн users: `components/OnlineUsers.tsx`
- Каналы: `components/ChannelSwitcher.tsx`

**База данных:**
- Путь: `cyberx-server/social-hub.db` (SQLite)
- Содержит: сообщения чата, пользователей, каналы

**Защита:**
- Зарезервированные ники: `admin.altufievo`, `admin.novokosino`
- Требуется пароль для входа в админку

---

## ⚔️ Arena - Система дуэлей

### Описание

**Arena** — это система организации **1v1 дуэлей** между игроками клуба. Игроки создают лобби, делают ставки и сразятся в различных играх.

### 🎯 Основной функционал

**Для игроков:**
- Создание лобби с выбором игры и ставки
- Присоединение к существующим лобби
- Система подтверждения результатов от админа

**Для администраторов:**
- Подтверждение результатов матчей
- Удаление лобби
- Мониторинг активных дуэлей

### 🔐 Админская панель Arena

**URL**:
- Продакшн: `https://cyberx-novokosino.ru/arena/admin`
- Локально: `http://localhost:3000/arena/admin`

> [!NOTE]
> Для доступа к Arena Admin требуется быть залогиненным через обычный `/arena` интерфейс

### Возможности админ-панели Arena

✅ **Просмотр активных лобби**
- Все текущие дуэли
- Информация о игроках, ставках, играх
- Статус лобби (ожидание/активная)

✅ **Подтверждение результатов**
- Кнопка "Подтвердить" для каждого лобби
- После подтверждения лобби удаляется

✅ **Удаление лобби**
- Кнопка "Удалить" для отмены дуэлей
- Мгновенное удаление из базы

### Процесс работы с дуэлями

1. **Игрок создаёт лобби** → система сохраняет в БД
2. **Другой игрок присоединяется** → лобби становится активным
3. **Игроки играют** → результат объявляется
4. **Админ подтверждает** → лобби закрывается
5. **Повторение** → новые дуэли

### Технические компоненты

**Файлы:**
- Админ-панель: `app/arena/admin/page.tsx`
- Основная арена: `app/arena/page.tsx`
- API лобби: `app/api/arena/lobbies/route.ts`
- API товаров: `app/api/arena/goods/route.ts`

**База данных:**
- Путь: `data/arena.db` (SQLite)
- Таблицы: `lobbies`, `goods`

**Схема лобби:**
```
- id: уникальный ID
- creator_nick: ник создателя
- creator_pc: номер ПК создателя
- joiner_nick: ник присоединившегося (или null)
- joiner_pc: номер ПК присоединившегося (или null)
- game: название игры
- bet_amount: сумма ставки
- status: waiting/active/confirmed
- created_at: время создания
```

---

## 💰 Управление ценами

### Описание

**Admin Prices** — административная панель для **редактирования цен** на сайте без изменения кода. Все данные хранятся в `data/prices.json`.

### 🔐 Доступ

**URL**:
- Продакшн: `https://cyberx-novokosino.ru/admin/prices`
- Локально: `http://localhost:3000/admin/prices`

**Авторизация:**
- Пароль из переменной `ADMIN_PASSWORD`
- Вводится один раз, сохраняется в сессии

### Что можно редактировать

#### 1. **Игровые зоны** (Zones)

Управление ценами для каждой зоны:
- **Общий зал**
- **VIP Bootcamp**
- **SOLO**

Каждая зона содержит:
- Категории тарифов (Будни/Выходные)
- Отдельные тарифы с временем и ценой
- Ночные тарифы (с бейджем 🌙)

#### 2. **Абонементы** (Abonnements)

Специальные предложения:
- Название (например, "Сутки", "50 часов")
- Цены для каждой зоны (Common/VIP/Solo)

#### 3. **Категории тарифов**

Группировка по времени:
- Будни / Выходные
- Ночные (с специальным оформлением)

### Как редактировать

**Изменение цен:**
1. Откройте админку `/admin/prices`
2. Найдите нужную зону/абонемент
3. Кликните на поле с ценой
4. Внесите изменения
5. Нажмите **"Сохранить"** вверху страницы

**Добавление новых элементов:**
- **Новая зона**: кнопка "+ Добавить зону"
- **Новая категория**: кнопка "+" рядом с категориями
- **Новый тариф**: кнопка "+" внутри категории
- **Новый абонемент**: кнопка "+ Добавить абонемент"

**Удаление:**
- Нажмите иконку 🗑️ рядом с элементом

### ⚠️ Важно для продакшена

> [!WARNING]
> Изменения применяются только на текущем сервере. При деплое из GitHub они могут быть перезаписаны.

**Рекомендуемый workflow:**
1. Измените цены через админку
2. Скачайте `data/prices.json` с сервера
3. Закоммитьте в репозиторий:
   ```bash
   git add data/prices.json
   git commit -m "Update prices via admin panel"
   git push
   ```

### Технические компоненты

**Файлы:**
- Админ-панель: `app/admin/prices/page.tsx`
- API prices: `app/api/admin/prices/route.ts`
- Данные: `data/prices.json`

**Публичная страница:**
- URL: `https://cyberx-novokosino.ru/prices`
- Автоматически обновляется после сохранения

---

## 📡 Технические детали

### Серверная архитектура

**Socket Server:**
- Путь: `cyberx-server/socket-server.js`
- Технология: Socket.IO
- Порт: 3001 (по умолчанию)

**Функции:**
- Реал-тайм чат в Social Hub
- Challenge система (вызовы между игроками)
- Уведомления о новых лобби в Arena
- Подсчёт онлайн пользователей

**Запуск:**
```bash
cd cyberx-server
node socket-server.js
```

### Базы данных

**Social Hub DB:**
- Путь: `cyberx-server/social-hub.db`
- Тип: SQLite
- Таблицы: messages, users, channels

**Arena DB:**
- Путь: `data/arena.db`
- Тип: SQLite
- Таблицы: lobbies, goods

### Переменные окружения

**Файл `.env.local` (локально):**
```env
# Пароль для всех админок
ADMIN_PASSWORD=your_secret_password

# Telegram Bot (для новостей)
TELEGRAM_BOT_TOKEN=your_bot_token

# Другие опциональные переменные
```

**На продакшене:**
Установите через настройки хостинга (Rusonix, Vercel и т.д.)

### API эндпоинты

**Social Hub:**
- Нет публичных API, всё через Socket.IO

**Arena:**
- `GET /api/arena/lobbies` — получить все лобби
- `POST /api/arena/lobbies` — создать лобби
- `POST /api/arena/lobbies/[id]` — присоединиться/подтвердить/удалить
- `GET /api/arena/goods` — получить товары

**Prices:**
- `GET /api/admin/prices` — получить цены
- `POST /api/admin/prices` — обновить цены

---

## 🛠️ Для разработчиков

### Локальный запуск

**Next.js фронтенд:**
```bash
npm run dev
```
Открывается на `http://localhost:3000`

**Socket сервер:**
```bash
cd cyberx-server
node socket-server.js
```
Работает на порту `3001`

### Структура проекта

```
cybernovokos2/
├── app/                    # Next.js app router
│   ├── admin/              # Админские панели
│   │   ├── login/          # Social Hub админ-вход
│   │   └── prices/         # Управление ценами
│   ├── social-hub/         # Социальная платформа
│   ├── arena/              # Система дуэлей
│   │   └── admin/          # Arena админка
│   └── api/                # API routes
├── components/             # React компоненты
├── cyberx-server/          # Socket.IO сервер
│   ├── socket-server.js     
│   └── social-hub.db       # БД Social Hub
├── data/                   # Статичные данные
│   ├── prices.json         # Цены
│   └── arena.db            # БД Arena
└── lib/                    # Утилиты
    └── socket-client.ts    # Socket клиент
```

---

## 🆘 Частые вопросы

**Q: Забыл пароль от админки**  
A: Пароль в переменной `ADMIN_PASSWORD`. Проверьте `.env.local` или настройки хостинга.

**Q: Social Hub чат не обновляется**  
A: Убедитесь, что Socket сервер запущен (`node socket-server.js` в `cyberx-server/`)

**Q: Цены не сохраняются**  
A: Проверьте права на запись для файла `data/prices.json`

**Q: Арена не показывает лобби**  
A: Проверьте наличие файла `data/arena.db` и права доступа

**Q: Как добавить нового администратора?**  
A: Все админы используют один пароль `ADMIN_PASSWORD`. Для Social Hub выбирается клуб при входе.

**Q: Можно ли изменить зарезервированные никнеймы?**  
A: Да, отредактируйте массив в `components/Onboarding.tsx` (строка 59)

---

## 📞 Техподдержка

При возникновении проблем:
1. Проверьте логи сервера (консоль где запущен `npm run dev`)
2. Проверьте логи Socket сервера
3. Проверьте консоль браузера (F12) на ошибки
4. Обратитесь к разработчику

---

## 📝 Дополнительные инструкции

- **Social Hub Admin**: `SOCIAL_HUB_ADMIN.md`
- **Управление ценами**: `ADMIN_GUIDE.md`
- **Arena**:в разработке

---

**Версия документа:** 1.0  
**Последнее обновление:** 30 января 2026
