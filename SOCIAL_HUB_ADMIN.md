# 🎮 Инструкция по доступу к Social Hub (Admin)

Административный доступ к социальному хабу CyberX для управления клубами.

---

## 📍 Доступ к админке Social Hub

### 🔗 Прямая ссылка для администраторов

**Продакшн**: `https://cyberx-novokosino.ru/admin/login`  
**Локально**: `http://localhost:3000/admin/login`

> [!IMPORTANT]
> Используйте **именно** `/admin/login`, а не обычный `/social-hub`

---

## 🔐 Процесс входа (Admin Login)

### Шаг 1: Откройте страницу авторизации

Перейдите по ссылке:
- **Продакшн**: https://cyberx-novokosino.ru/admin/login
- **Локально**: http://localhost:3000/admin/login

### Шаг 2: Выберите клуб

На странице входа выберите один из клубов:
- **Алтуфьево** (желтая подсветка)
- **Новокосино** (желтая подсветка)

### Шаг 3: Введите пароль управления

Введите админский пароль в поле **"Пароль управления"**

> [!WARNING]
> Пароль хранится в переменной окружения или настройках сервера. Свяжитесь с разработчиком для получения пароля.

### Шаг 4: Авторизация

Нажмите кнопку **"АВТОРИЗОВАТЬСЯ"**

После успешной авторизации вы автоматически перенаправитесь на `/social-hub` с правами администратора.

---

## 🎯 Возможности после входа

После авторизации вы получаете доступ к:

- ✅ **Общему чату** клуба
- ✅ **Новостной ленте** из Telegram-каналов
- ✅ **Списку онлайн-пользователей**
- ✅ **Промокодам и акциям**
- ✅ **Дуэлям с игроками**
- ✅ **Административным функциям** (если реализованы)

---

## 🔒 Безопасность

- ✅ Доступ защищен паролем на уровне сервера
- ✅ Аутентификация через Socket.IO с проверкой пароля
- ✅ Неавторизованные попытки логируются
- ✅ Страница `/admin/login` требует обязательной авторизации

### Технические детали

**Файлы:**
- Страница входа: `app/admin/login/page.tsx`
- Socket функция: `socketClient.authenticateAdmin(password, club, callback)`

**Переменные окружения:**

Проверьте файл `.env.local` (локально) или настройки хостинга:

```env
# Пароль для админ-входа в Social Hub
ADMIN_PASSWORD=ваш_секретный_пароль
```

---

## 🆘 Частые вопросы

**Q: Где взять админский пароль?**  
A: Пароль находится в переменной окружения `ADMIN_PASSWORD`. Проверьте `.env.local` локально или настройки хостинга на продакшене. Свяжитесь с разработчиком.

**Q: Ввожу пароль, но не пускает**  
A: Убедитесь, что:
1. Выбран правильный клуб
2. Пароль введён без ошибок
3. Переменная `ADMIN_PASSWORD` установлена на сервере

**Q: После входа вижу обычный Social Hub**  
A: Это нормально. Админская авторизация даёт вам доступ к управлению, но интерфейс остаётся тем же Social Hub.

**Q: Какая разница между admin.altufievo и /admin/login?**  
A: 
- `admin.altufievo` / `admin.novokosino` — зарезервированные никнеймы (НЕ используются для админ-доступа)
- `/admin/login` — **правильный путь** для входа администраторов с паролем

**Q: Можно ли войти через обычный /social-hub?**  
A: Нет. Обычный `/social-hub` показывает onboarding для обычных пользователей. Администраторы должны использовать `/admin/login`.

---

## 📋 Техническая информация

### Связанные файлы

- **Админ-вход**: `app/admin/login/page.tsx`
- **Socket клиент**: `lib/socket-client.ts`
- **Серверная часть**: `cyberx-server/socket-server.js`
- **База данных**: `cyberx-server/social-hub.db`

### Для разработки

Локальный запуск:
```bash
# Next.js фронтенд
npm run dev

# Socket сервер (в папке cyberx-server)
node socket-server.js
```

---

## 📞 Дополнительно

### Обычный вход пользователей

Обычные пользователи (не админы) заходят через:
- `https://cyberx-novokosino.ru/social-hub`

Там проходят onboarding с выбором героя и вводом никнейма.

### Зарезервированные никнеймы

Следующие никнеймы защищены от использования обычными пользователями:
- `admin.altufievo`
- `admin.novokosino`

> [!NOTE]
> Эти никнеймы не используются для админ-входа! Они просто зарезервированы для системы.

---

При возникновении проблем проверьте логи сервера или обратитесь к разработчику.
